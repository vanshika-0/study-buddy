'use client'

// The existing Pomodoro URL now opens the Focus Session experience. The
// session still uses the existing /api/pomodoro/study-time backend endpoint.
import FocusPage from '../focus/page'

export default FocusPage
